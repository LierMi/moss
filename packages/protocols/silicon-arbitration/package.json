{
  "name": "@themoss/protocol-silicon-arbitration",
  "version": "0.0.1",
  "private": true,
  "description": "Moss Protocol adapter for Silicon Labor Arbitration createTask on Monad Testnet",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap --clean --target es2022 --external zod --external viem",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@themoss/core": "workspace:*",
    "viem": "^2.54.3"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "~5.9.0",
    "vitest": "^3.2.6"
  }
}
